import java.io.*;
import java.net.*;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.List;
import javax.xml.parsers.DocumentBuilderFactory;
import javax.xml.parsers.DocumentBuilder;
import org.w3c.dom.Document;
import org.w3c.dom.NodeList;
import org.w3c.dom.Node;
import org.w3c.dom.Element;

public class Grouper {

    // We want to read three incoming jobs.
    // We need to skip jobs and save to arraylist.
    // PSHJ - requests to get the next job, skipping the current job without
    // scheduling it

    // We need some global variables
    // int Group1EstTime;
    // int Group2EstTime;
    // ArrayList<String> Group1 = new ArrayList<String>();
    // ArrayList<String> Group2 = new ArrayList<String>();
    // HashMap<String, Integer> AllGroupEstTime = new HashMap<String, Integer>();

    // We need some getters
    // get GroupEstimatedTime(group)

    // We need some setters
    // set Group1EstTime(group)
    // take all estimated times and set the average
    // set Group2EstTime(group)

    // function to sort group based on average estimated run time
    // allgroupsorter() {
    // look at keyvalue pairs in the hashmap,
    // if group1 > group2
    // return group 2
    // else return group 1
    // }

    // function to place jobs into arraylists
    // REDY
    // READline
    // din.append

    // function to sort the arraylists
    // target the estimated run time.

}